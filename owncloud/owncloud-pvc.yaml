# Source: owncloud/templates/owncloud-pvc.yaml
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: owncloud-owncloud
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "8Gi"
---
